import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.JavascriptExecutor;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;
import java.time.Duration;
package selenium;


public class MoodleDeleteQuiz {
    private WebDriver driver;
    private WebDriverWait wait;

    public MoodleDeleteQuiz(WebDriver driver, WebDriverWait wait) {
        this.driver = driver
        this.wait = wait
    }

    public void navigateToCourses() throws InterruptedException {
    }

    public void chooseCourse(int course) throws InterruptedException {

    }

    public void navigateToQuizes() throws InterruptedException {
    }

    public void chooseQuizAndDelete(int quiz) throws InterruptedException {
        WebElement navigatetoLogin = driver.findElement(By.xpath(//login button xpath));
        myAccountElement.click();
        WebElement usernameElement = driver.findElement(By.xpath(//xpath for username textbox));
        usernameElement.sendKeys(username);
        WebElement usernameElement = driver.findElement(By.xpath(//xpath for username textbox));
        usernameElement.sendKeys(username);
        WebElement usernameElement = driver.findElement(By.xpath(//xpath for username textbox));
        usernameElement.sendKeys(username);
        WebElement usernameElement = driver.findElement(By.xpath(//xpath for username textbox));
        usernameElement.sendKeys(username);
    }
}
